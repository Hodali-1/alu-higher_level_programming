# Python - Input/Output

This project covers reading and writing files in Python: text files,
JSON serialization/deserialization, saving and loading Python objects,
and using JSON to persist class instances to disk.

## Requirements
- Editors: `vi`, `vim`, `emacs`
- Interpreted on Ubuntu 20.04 LTS, python3 (version 3.8.5)
- All files end with a new line
- First line of every file: `#!/usr/bin/python3`
- Code follows pycodestyle (version 2.7.*)
- All files are executable
- Every module, class, and function has a real, descriptive docstring
